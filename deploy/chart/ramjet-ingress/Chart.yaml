apiVersion: v2
name: ramjet-ingress
description: The ramjet-ingress controller — a Kubernetes Ingress controller and HTTP data plane in one process.
type: application

# The chart and the thing it deploys are released together and version in
# lockstep, so these two are deliberately the same number.
version: 0.1.0
appVersion: "0.1.0"

kubeVersion: ">=1.19.0-0"

keywords:
  - ingress
  - proxy
  - networking
home: https://github.com/sofelia-ai/ramjet-ingress
sources:
  - https://github.com/sofelia-ai/ramjet-ingress
