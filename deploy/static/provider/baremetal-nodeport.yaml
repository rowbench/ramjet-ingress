# ramjet-ingress — baremetal-nodeport
#
# Generated. Do not edit this file: edit deploy/provider/baremetal-nodeport/values.yaml
# and re-run deploy/render.sh, or every change here is lost the next time
# anyone does.
#
#     kubectl apply -f deploy/static/provider/baremetal-nodeport.yaml
#
# Equivalent to:
#
#     helm install ramjet deploy/chart/ramjet-ingress \
#       --namespace ramjet-ingress --create-namespace \
#       -f deploy/provider/baremetal-nodeport/values.yaml
#
# The values file carries the reasoning behind each annotation below; this file
# deliberately carries none of it, because a comment here has nowhere to live
# across a re-render. Read deploy/README.md and the preset before deploying.
---
apiVersion: v1
kind: Namespace
metadata:
  name: ramjet-ingress
  labels:
    app.kubernetes.io/name: ramjet-ingress
    app.kubernetes.io/instance: ramjet
---
---
# Source: ramjet-ingress/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ramjet-ramjet-ingress
  namespace: ramjet-ingress
  labels:
    helm.sh/chart: ramjet-ingress-0.1.1
    app.kubernetes.io/name: ramjet-ingress
    app.kubernetes.io/instance: ramjet
    app.kubernetes.io/version: "0.1.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
automountServiceAccountToken: true
---
# Source: ramjet-ingress/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ramjet-ramjet-ingress
  labels:
    helm.sh/chart: ramjet-ingress-0.1.1
    app.kubernetes.io/name: ramjet-ingress
    app.kubernetes.io/instance: ramjet
    app.kubernetes.io/version: "0.1.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
rules:
  - apiGroups:
      - networking.k8s.io
    resources:
      - ingresses
    verbs:
      - get
      - list
      - watch
      # Canary auto-promotion only, and only ever on annotations. An
      # annotation lives in `metadata`, so the `ingresses/status` rule below
      # cannot carry it and this has to be the primary resource. `update` is
      # deliberately absent: a server-side-apply patch names the three keys it
      # owns, where an update would send a whole object and could clobber a
      # concurrent edit to a field this controller has no business touching.
      - patch
  - apiGroups:
      - networking.k8s.io
    resources:
      - ingressclasses
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - networking.k8s.io
    resources:
      - ingresses/status
    verbs:
      - get
      - patch
      - update
  - apiGroups:
      - ""
    resources:
      - services
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - discovery.k8s.io
    resources:
      - endpointslices
    verbs:
      - get
      - list
      - watch
  # `patch` alongside `create` because the Events API's own aggregation path
  # updates an existing Event rather than writing a new one, and a rule that
  # only allowed `create` would work right up until it did not.
  - apiGroups:
      - events.k8s.io
    resources:
      - events
    verbs:
      - create
      - patch
---
# Source: ramjet-ingress/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ramjet-ramjet-ingress
  labels:
    helm.sh/chart: ramjet-ingress-0.1.1
    app.kubernetes.io/name: ramjet-ingress
    app.kubernetes.io/instance: ramjet
    app.kubernetes.io/version: "0.1.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: ramjet-ramjet-ingress
subjects:
  - kind: ServiceAccount
    name: ramjet-ramjet-ingress
    namespace: ramjet-ingress
---
# Source: ramjet-ingress/templates/admin-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ramjet-ramjet-ingress-admin
  namespace: ramjet-ingress
  labels:
    helm.sh/chart: ramjet-ingress-0.1.1
    app.kubernetes.io/name: ramjet-ingress
    app.kubernetes.io/instance: ramjet
    app.kubernetes.io/version: "0.1.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "10254"
    prometheus.io/path: /metrics
spec:
  type: ClusterIP
  # A replica that is not ready is exactly the one whose metrics and /readyz
  # someone wants to look at. Endpoint readiness gating is right for the
  # traffic Service and wrong for this one.
  publishNotReadyAddresses: true
  ports:
    - name: admin
      port: 10254
      targetPort: admin
      protocol: TCP
  selector:
    app.kubernetes.io/name: ramjet-ingress
    app.kubernetes.io/instance: ramjet
---
# Source: ramjet-ingress/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ramjet-ramjet-ingress
  namespace: ramjet-ingress
  labels:
    helm.sh/chart: ramjet-ingress-0.1.1
    app.kubernetes.io/name: ramjet-ingress
    app.kubernetes.io/instance: ramjet
    app.kubernetes.io/version: "0.1.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
      nodePort: 30080
    - name: https
      port: 443
      # Usually the TLS listener, but not always: where the load balancer
      # terminates TLS this points at `http`, and the port named https here
      # carries plaintext that was HTTPS one hop ago. The name is the Service
      # port's, and Service port names are what provider annotations like
      # aws-load-balancer-ssl-ports select on, so it stays `https` either way.
      targetPort: https
      protocol: TCP
      nodePort: 30443
  selector:
    app.kubernetes.io/name: ramjet-ingress
    app.kubernetes.io/instance: ramjet
---
# Source: ramjet-ingress/templates/controller.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ramjet-ramjet-ingress
  namespace: ramjet-ingress
  labels:
    helm.sh/chart: ramjet-ingress-0.1.1
    app.kubernetes.io/name: ramjet-ingress
    app.kubernetes.io/instance: ramjet
    app.kubernetes.io/version: "0.1.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
spec:
  # Hard-coded, not a value. There is no leader election yet: every replica
  # would run its own status writer, patching the same Ingresses with the same
  # field manager on its own schedule. See the note in values.yaml.
  replicas: 1
  strategy:
    # One replica and a status writer means Recreate rather than RollingUpdate:
    # a surge pod would put two writers on the same objects for the length of
    # the rollout, which is the state the replica count exists to prevent.
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: ramjet-ingress
      app.kubernetes.io/instance: ramjet
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ramjet-ingress
        app.kubernetes.io/instance: ramjet
    spec:
      serviceAccountName: ramjet-ramjet-ingress
      # An explicit dnsPolicy wins; otherwise hostNetwork decides it. A
      # host-network pod under plain ClusterFirst inherits the *node's*
      # resolv.conf and cannot resolve a cluster Service, so leaving this to
      # chance would make the controller's API access depend on how the node
      # happens to be configured.
      dnsPolicy: ClusterFirst
      # Longer than controller.shutdownGrace, so the process ends by finishing
      # its drain rather than by being killed part-way through one.
      terminationGracePeriodSeconds: 45
      securityContext:
        fsGroup: 65532
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: controller
          image: "sofelia/ramjet-ingress:0.1.1"
          imagePullPolicy: IfNotPresent
          args:
            - --http=:8080
            - --https=:8443
            - --admin=:10254
            - --engine=hyper
            - --ingress-class=ramjet
            - --publish-service=ramjet-ingress/ramjet-ramjet-ingress
            - --connect-timeout=5
            - --response-timeout=60
            - --max-connect-attempts=3
            - --shutdown-grace=30
            - --history-size=10
          env:
            - name: RUST_LOG
              value: "info,kube=warn"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          # No hostPort even under hostNetwork: the API server defaults hostPort
          # to containerPort for a host-network pod, and the scheduler's port
          # conflict check reads it from there. Writing it out again would only
          # be a second place to keep in sync.
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: https
              containerPort: 8443
              protocol: TCP
            - name: admin
              containerPort: 10254
              protocol: TCP
          # /healthz answers whenever the process is answering; /readyz waits
          # for a compiled route table. Restarting on the former and gating
          # traffic on the latter is the whole distinction: a replica that has
          # not finished its first list should be kept out of the Service, not
          # killed and made to start over.
          livenessProbe:
            httpGet:
              path: /healthz
              port: admin
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: admin
            failureThreshold: 3
            initialDelaySeconds: 2
            periodSeconds: 5
            timeoutSeconds: 3
          resources:
            limits:
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              add:
              - NET_BIND_SERVICE
              drop:
              - ALL
            readOnlyRootFilesystem: true
---
# Source: ramjet-ingress/templates/ingressclass.yaml
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: ramjet
  labels:
    helm.sh/chart: ramjet-ingress-0.1.1
    app.kubernetes.io/name: ramjet-ingress
    app.kubernetes.io/instance: ramjet
    app.kubernetes.io/version: "0.1.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: controller
spec:
  controller: ramjet.dev/ingress
